#!/bin/env Rscript


#' Multiline (auto) comment
#'

# single line comment

# packages to install
install.packages(c("ggplot2", "plyr", "reshape2"))

# to get the information for a package
library(help = "base")

# information about a function
?read.table

install.packages(c("randomForest"))
